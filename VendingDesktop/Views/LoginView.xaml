<UserControl x:Class="VendingDesktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#f0f2f5">
        <Border Background="White" CornerRadius="12" Padding="40" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource ShadowEffect}">
            <StackPanel>
                <TextBlock Text="Авторизация" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBlock Text="Email" Margin="0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                <TextBlock Text="Пароль" Margin="0,10,0,5"/>
                <PasswordBox x:Name="PwdBox" Padding="8" PasswordChanged="PwdBox_PasswordChanged"/>
                <TextBlock Text="{Binding Error}" Foreground="Red" FontSize="12" Margin="0,5"/>
                <Button Content="Войти" Command="{Binding LoginCommand}" Background="#4a90d9" Foreground="White" Padding="10" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
