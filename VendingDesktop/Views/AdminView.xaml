<UserControl x:Class="VendingDesktop.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Администрирование торговых автоматов" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5" Padding="5" ToolTip="Фильтр по названию"/>
            <Button Content="🔍 Поиск" Command="{Binding SearchCommand}" Margin="5" Padding="10,5" Background="#4a90d9" Foreground="White"/>
            <Button Content="➕ Добавить ТА" Command="{Binding AddCommand}" Margin="5" Padding="10,5" Background="Green" Foreground="White"/>
            <ComboBox Width="100" Margin="5" SelectedItem="{Binding PageSize}" ToolTip="Строк на странице">
                <ComboBoxItem Content="10"/><ComboBoxItem Content="20"/><ComboBoxItem Content="50"/>
            </ComboBox>
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" Margin="10,0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Machines}" SelectedItem="{Binding SelectedMachine}" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#f5f5f5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding MachineID}" Width="50"/>
                <DataGridTextColumn Header="Название автомата" Binding="{Binding Location}" Width="250"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="150"/>
                <DataGridTextColumn Header="Компания" Binding="{Binding CompanyName}" Width="120"/>
                <DataGridTextColumn Header="Модем" Binding="{Binding ModemID}" Width="80"/>
                <DataGridTextColumn Header="Адрес/Место" Binding="{Binding Location}" Width="200"/>
                <DataGridTextColumn Header="В работе с" Binding="{Binding DateOfCommissioning, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTemplateColumn Header="Действия" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" ToolTip="Редактировать" Margin="2" Padding="5,2"/>
                                <Button Content="🗑️" ToolTip="Удалить" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Margin="2" Padding="5,2" Background="Red" Foreground="White"/>
                                <Button Content="🔗 Отвязать" ToolTip="Отвязать модем" Command="{Binding DataContext.DetachModemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Margin="2" Padding="5,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="◀ Назад" Command="{Binding PrevPageCommand}" Margin="5" Padding="10,5"/>
            <TextBlock Text="{Binding Page}" VerticalAlignment="Center" Margin="5"/>
            <Button Content="Вперед ▶" Command="{Binding NextPageCommand}" Margin="5" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
